# 路線資訊功能說明

## 功能概述

在時間表視圖中，系統會自動顯示兩個相鄰景點之間的路線資訊，包括：
- 🚗 行車距離
- ⏱️ 預計行車時間
- 🧭 Google Maps 導航連結

## 功能特點

### 1. 自動計算路線
- 使用 Google Maps Distance Matrix API 計算實際行車距離和時間
- 只顯示有座標資訊的景點之間的路線
- 自動排除自訂活動（如交通、休息等）

### 2. 視覺呈現
- 路線資訊卡片位於兩個景點之間
- 漸層藍色背景，與時間軸整體設計協調
- 清晰顯示距離（公里）和時間（分鐘）
- Hover 時顯示起點和終點名稱的提示

### 3. 一鍵導航
- 點擊「導航」按鈕直接開啟 Google Maps
- 自動設定起點和終點
- 預設使用駕駛模式
- 在新分頁開啟，不影響當前頁面

## 技術實作

### 新增組件

#### RouteInfo.tsx
```typescript
interface RouteInfoProps {
  from: {
    name: string;
    coordinates?: Coordinates;
  };
  to: {
    name: string;
    coordinates?: Coordinates;
  };
}
```

**主要功能：**
- 使用 Google Maps Distance Matrix API 獲取路線資訊
- 顯示距離、時間和導航按鈕
- 處理載入狀態和錯誤狀態

### 修改組件

#### TimelineSchedule.tsx
**新增功能：**
- 計算排序後的景點列表（排除自訂活動）
- 在相鄰景點之間插入 RouteInfo 組件
- 根據景點位置動態調整路線資訊卡片位置

#### useGoogleMaps.ts
**更新：**
- 新增 'geometry' library 到 Google Maps API 載入清單
- 確保 Distance Matrix API 可用

## 使用方式

### 前置條件
1. 景點必須有座標資訊（經緯度）
2. Google Maps API Key 必須正確設定
3. API Key 需啟用 Distance Matrix API

### 操作流程
1. 在時間表中安排景點
2. 確保景點有座標資訊（透過 Google Places 搜尋新增的景點會自動帶入）
3. 系統自動顯示相鄰景點間的路線資訊
4. 點擊「導航」按鈕開啟 Google Maps 導航

## 顯示邏輯

路線資訊會在以下條件下顯示：
- ✅ 兩個景點都有座標資訊
- ✅ 兩個景點都不是自訂活動
- ✅ 兩個景點在時間上相鄰
- ✅ Google Maps API 已成功載入

不會顯示路線資訊的情況：
- ❌ 景點沒有座標資訊
- ❌ 景點是自訂活動（如「開車」、「休息」等）
- ❌ 是當日最後一個景點（沒有下一個目的地）

## 範例場景

### 情境 1：完整行程
```
09:00 - 10:00  落磯山脈國家公園
                 ↓ 32.5 km · 25分鐘 [導航]
10:30 - 12:00  露易絲湖
                 ↓ 15.2 km · 18分鐘 [導航]
12:30 - 13:30  湖畔餐廳
```

### 情境 2：包含自訂活動
```
09:00 - 10:00  班夫小鎮
                 ↓ 58.1 km · 45分鐘 [導航]
11:00 - 12:00  落磯山脈國家公園
12:00 - 14:00  🚗 開車（自訂活動 - 不顯示路線）
14:00 - 16:00  露易絲湖
```

## 注意事項

1. **API 額度**
   - Distance Matrix API 有每日請求限制
   - 建議監控 API 使用量
   - 考慮實作快取機制減少重複請求

2. **載入時間**
   - 首次載入時需要等待 API 回應
   - 顯示「計算路線中...」載入狀態
   - 通常在 1-2 秒內完成

3. **錯誤處理**
   - API 請求失敗時不顯示路線資訊
   - 不會影響其他功能正常使用
   - 靜默失敗，不顯示錯誤訊息

4. **座標精確度**
   - 使用 Google Places API 的座標最準確
   - 手動輸入的座標可能不精確
   - 建議使用搜尋功能新增景點

## 未來改進方向

- [ ] 支援多種交通方式（步行、大眾運輸、騎車）
- [ ] 顯示路線預覽地圖
- [ ] 快取路線資訊減少 API 請求
- [ ] 支援途經點的多段路線規劃
- [ ] 顯示即時路況資訊
- [ ] 整合 WeeklyTimeline 週視圖
